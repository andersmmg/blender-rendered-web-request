# Blender Rendered Web Request Addon

Makes a basic HTTP request when a render is complete in Blender

### Note: Make sure you change the url in `render_request.py` to whatever service you want it to call
